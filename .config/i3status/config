general {
        colors = true
        interval = 5
}

order += "tztime local"
order += "volume master"

volume master {
        format = "♪: %volume"
        format_muted = "♪: muted (%volume)"
}

tztime local {
        format = "%d-%m-%Y %H:%M:%S"
}
