#!/bin/bash

xinput --list | grep -i 'pointer' | grep -v 'XTEST' | grep -oP 'id=\K\d+' | while read -r id; do
    device_name=$(xinput --list --name-only "$id")
    
    xinput --set-prop "$id" "libinput Accel Profile Enabled" 0 1 0 2>/dev/null
done
